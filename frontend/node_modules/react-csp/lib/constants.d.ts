import { ConfigExtension } from "./types";
declare let filename: string;
declare const baseDir: string;
declare const configPath: (extension: ConfigExtension) => string;
declare const htmlPath: string;
export { baseDir, configPath, htmlPath, filename };
